# Site Preview

This directory contains a small static preview site used as a repository presentation asset.

Files:

- `index.html`
- `script.js`
- `styles.css`

It is not part of the core skill runtime. The main repository deliverables live under `skills/` and `docs/`.
